// import { fromJS } from 'immutable';
// import { makeSelectLandingPageDomain } from '../selectors';

// const selector = makeSelectLandingPageDomain();

describe('makeSelectLandingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
