// import { fromJS } from 'immutable';
// import { makeSelectServiciosListDomain } from '../selectors';

// const selector = makeSelectServiciosListDomain();

describe('makeSelectServiciosListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
