// import { fromJS } from 'immutable';
// import { makeSelectProfileWorkerDomain } from '../selectors';

// const selector = makeSelectProfileWorkerDomain();

describe('makeSelectProfileWorkerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
