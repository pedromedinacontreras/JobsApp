// import { fromJS } from 'immutable';
// import { makeSelectRecoverPasswordDomain } from '../selectors';

// const selector = makeSelectRecoverPasswordDomain();

describe('makeSelectRecoverPasswordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
